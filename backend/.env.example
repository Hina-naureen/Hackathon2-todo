# backend/.env.example
# Copy this file to backend/.env and fill in real values.
# Never commit .env to git.

# ── Database ────────────────────────────────────────────────────────────────
# Neon DB (production):
# DATABASE_URL=postgresql+psycopg2://user:password@host.neon.tech/db?sslmode=require
#
# SQLite (local dev, created automatically if DATABASE_URL is not set):
# DATABASE_URL=sqlite:///./dev.db
DATABASE_URL=

# ── Authentication ───────────────────────────────────────────────────────────
# Must match BETTER_AUTH_SECRET in frontend/.env.local.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
BETTER_AUTH_SECRET=

# ── Development helpers ──────────────────────────────────────────────────────
# Set to "true" to bypass JWT validation in local development ONLY.
# NEVER set this in production.
AUTH_DISABLED=false

# ── CORS ────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins (no trailing slash).
# ALLOWED_ORIGINS=http://localhost:3000,https://your-prod-domain.com
ALLOWED_ORIGINS=http://localhost:3000
