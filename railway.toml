[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"
buildContext = "."

[deploy]
startCommand = "sh -c 'uv run alembic upgrade head && uv run uvicorn src.app:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
